import { pluralItemView } from 'frontend/helpers/plural-item-view';
import { module, test } from 'qunit';

module('Unit | Helper | plural item view');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pluralItemView([42]);
  assert.ok(result);
});
